<h1>My Tasks</h1>

<% @tasks.each do |task| %>
  <h2><%= task.title %></h2>
  <%= link_to task.title, task_path(task) %>
  <p><%= task.details %></p>
  <p><%= task.completed %></p>

  <%= link_to "Delete", task_path(task),
    method: :delete,
    data: { confirm: "Are you sure?" } %>
<% end %>


